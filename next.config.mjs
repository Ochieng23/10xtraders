/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // output: "export",
  // assetPrefix: "/nextjs",
  // basePath: "/nextjs",
  images: {
    domains: ["res.cloudinary.com"],
  },
};

export default nextConfig;
